import { AssetPreviewPipe } from './asset-preview.pipe';

describe('AssetPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
